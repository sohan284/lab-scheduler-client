import domain from "./domain";

const baseUrl = {
  users: `${domain}/users`,
  login: `${domain}/login`,
  task: `${domain}/tasks`,
  scheduledtasks: `${domain}/scheduledtasks`,
};

export default baseUrl;
